import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Shakuns Freight | Premium Logistics & Supply Chain Solutions",
  description: "Experience world-class logistics with Shakuns Freight. Authorized partner of DHL, FedEx, and DTDC. Reliable, fast, and tech-forward freight services.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
      <body className="min-h-screen selection:bg-accent/30">
        {children}
      </body>
    </html>
  );
}
